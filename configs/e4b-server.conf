# E4B Sub-agent Server Configuration
# Modelo pequeño para tools simples (CPU-only)

# Model path
MODEL_PATH=/home/darkseid/llama.cpp/ai-models/google_gemma-4-E4B-it-Q4_K_M.gguf

# GPU layers: 0 = CPU-only (ahorra VRAM para modelo principal)
NGL=0

# Server settings
HOST=0.0.0.0
PORT=9091

# Context (pequeño, suficiente para tools)
CTX_SIZE=8192

# Extra args (optional)
EXTRA_ARGS=
